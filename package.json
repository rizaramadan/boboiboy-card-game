{
  "name": "boboiboy-card-game",
  "version": "1.0.0",
  "description": "Mobile browser runner game with card scanning",
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-manifest.js && vite",
    "build": "node scripts/generate-manifest.js && tsc && vite build",
    "preview": "vite preview",
    "manifest": "node scripts/generate-manifest.js"
  },
  "dependencies": {
    "phaser": "^3.80.1",
    "tesseract.js": "^5.0.5"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  }
}
